version: "3.8"

services:
  naagostone:
    image: ghcr.io/${USERNAME_LOWERCASE:-tancred}/naagostone:latest
    container_name: naagostone-api
    ports:
      - "3001:3001"
    restart: unless-stopped
    networks:
      - naagostone-network

networks:
  naagostone-network:
    driver: bridge
